# Applications protocol

The Applications protocol is standard way of finding apps and software

The Applications protocol does not use just one service to find software. It instead has software publishers host there own apps themselves. Then the users machine looks up through all of these different servers to find the desired software.